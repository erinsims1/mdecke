version: 2
jobs:
  build:
    docker:
      - image: ubuntu
    steps:
      - checkout
      - run: echo "Ran test job"
  test:
    docker:
      - image: ubuntu
    steps:
      - checkout
      - run: echo "Ran test job"
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
        requires:
          - build
