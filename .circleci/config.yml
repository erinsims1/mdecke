version: 2
jobs:
  build:
    docker:
      - image: ubuntu
    steps:
      - checkout
      - run: apt-get install build-essential
      - run: cd assignments/bigint &&
          make
  test:
    docker:
      - image: ubuntu
    steps:
      - checkout
      - run: echo "Ran test job" &&
          ls assignments/bigint
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test:
          requires:
            - build
